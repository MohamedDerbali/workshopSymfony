<form method="post" action="{{ path('app_enfant_delete', {'idEnfant': enfant.idEnfant}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ enfant.idEnfant) }}">
    <button class="btn">Delete</button>
</form>
